@if (Request.IsAuthenticated)
{<text>
     <strong>@Html.ActionLink(@User.Identity.Name, "MyProfile", "Account")</strong>
     <strong>@Html.ActionLink("System", "Index", "System")</strong>
					[@Html.ActionLink("Log Off", "LogOff", "Account")]
 </text>
}
else
{
    @:[@Html.ActionLink("Log On", "LogOn", "Account")]
}
